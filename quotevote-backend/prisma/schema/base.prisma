// Base configuration for Prisma
// Contains generator and datasource settings

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}
